vimtips is a project inspired by fortune software to view vimtips.

The vimtips file is a text file with the format of fortune command.

Install the plugin and fortunes directory in your .vim directory.
